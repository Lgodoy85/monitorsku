_schema-version: "3.2"
ID: geoskumonitor
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: geoskumonitor-destination-content
  type: com.sap.application.content
  requires:
  - name: geoskumonitor-destination-service
    parameters:
      content-target: true
  - name: geoskumonitor-repo-host
    parameters:
      service-key:
        name: geoskumonitor-repo-host-key
  - name: geoskumonitor-uaa
    parameters:
      service-key:
        name: geoskumonitor-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: geoskumonitor_html_repo_host
          ServiceInstanceName: geoskumonitor-html5-srv
          ServiceKeyName: geoskumonitor-repo-host-key
          sap.cloud.service: geoskumonitor
        - Authentication: OAuth2UserTokenExchange
          Name: geoskumonitor_uaa
          ServiceInstanceName: geoskumonitor-xsuaa-srv
          ServiceKeyName: geoskumonitor-uaa-key
          sap.cloud.service: geoskumonitor
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: geoskumonitor-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: geoskumonitor-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - geoskumonitor.zip
      name: geoskumonitor
      target-path: resources/
- name: geoskumonitor
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: geoskumonitor-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: geoskumonitor-destination-service
    service-plan: lite
- name: geoskumonitor-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: geoskumonitor-xsuaa-srv
    service-plan: application
- name: geoskumonitor-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: geoskumonitor-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
